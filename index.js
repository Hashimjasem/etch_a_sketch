// generate a grid
const gridContainer = document.getElementById("container")

let gridsquare = "<div class='box'>.</div>"

let grid = function(gridContainer){
    // how do i create multiple divs
    function creategridsquare(){
    for (let i=0; 1 < 80; i++) {
        gridContainer.insertAdjacentHTML("afterend, html")
    }}

}